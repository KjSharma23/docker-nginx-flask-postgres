# Dockerized Flask Application with Nginx Reverse Proxy

## 📌 Project Overview
This project demonstrates a production-style containerized web application using Docker and Docker Compose.  
A Flask backend application runs behind an Nginx reverse proxy and connects to a PostgreSQL database.

The application is deployed on an AWS EC2 instance.

---

## 🧱 Architecture
